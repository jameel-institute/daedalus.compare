# daedalus.compare: Run Multiple DAEDALUS Scenarios of Pandemic Outcomes

*daedalus.compare* is a helper package that wraps the *daedalus* package
for epi-macroeconomic modelling, and is primarily focused on making it
easy to run multiple scenarios of pandemic mitigation strategies along
with uncertainty in infection parameters.

## Installation

You can install the development version of *daedalus.compare* from the
Jameel Institute R-universe (recommended) or from GitHub.

``` r
# installation from R-universe
install.packages(
  "daedalus.compare", 
  repos = c(
    "https://jameel-institute.r-universe.dev", "https://cloud.r-project.org"
  )
)

# installation from GitHub using {pak}
install.packages("pak")
pak::pak("jameel-institute/daedalus.compare")
```

You can also install

## Quick start

This example shows how to model multiple pandemic response scenarios in
the U.K. with uncertainty in \\R_0\\ of an H1N1-like infection. **Note
that** *daedalus.compare* only supports running
[`daedalus::daedalus_multi_infection()`](https://jameel-institute.github.io/daedalus/reference/daedalus_multi_infection.html)
using
[`daedalus.compare::run_scenarios()`](https://jameel-institute.github.io/daedalus.compare/reference/run_scenarios.md)
at present.

``` r
library(daedalus)
library(daedalus.compare)

# make list of infection objects with R0 of 1.0 -- 2.0 with skewed distribution
set.seed(1) # for reproducible results
infection_list <- make_infection_samples(
  "influenza_2009",
  param_distributions = list(
    r0 = distributional::dist_beta(2, 5)
  ),
  param_ranges = list(
    r0 = c(1.0, 2.0)
  ),
  samples = 10
)

elimination <- daedalus_timed_npi(
  start_time = 10,
  end_time = 100,
  openness = list(
    daedalus.data::closure_strategy_data[["elimination"]]
  ), # must be a list
  "GBR" # must specify country
)

# run multiple scenarios of outputs
output <- run_scenarios(
  "GBR", infection_list,
  response = list(
    none = NULL,
    elimination = elimination
  ),
  time_end = 200
)

# view output which is a data.table
output
#>       response time_end     output
#>         <char>    <num>     <list>
#> 1:        none      200 <list[10]>
#> 2: elimination      200 <list[10]>

# get epi-curve data
disease_tags <- sprintf("sample_%i", seq_along(infection_list))
epi_curves <- get_epicurve_data(output, disease_tags)
```

``` r
# plot epi-curve data showing daily hospitalisations
library(dplyr)
#> 
#> Attaching package: 'dplyr'
#> The following objects are masked from 'package:stats':
#> 
#>     filter, lag
#> The following objects are masked from 'package:base':
#> 
#>     intersect, setdiff, setequal, union
library(ggplot2)

epi_curves %>%
  filter(measure == "daily_hospitalisations") %>%
  ggplot(aes(time, value)) +
  geom_line(
    aes(col = response, group = interaction(tag, response))
  ) +
  labs(
    x = "Time", y = "Daily hospitalisations",
    col = "Mitigation strategy"
  )
```

![](reference/figures/README-unnamed-chunk-3-1.png)

## Related projects

*daedalus.compare* is intended to be a helper package for the
macro-economic and epidemiological [modelling package
*daedalus*](https://github.com/jameel-institute/daedalus), also
developed at the Jameel Institute.

The DAEDALUS model was originally developed in Haw et al.
([2022](#ref-haw2022)), and the R implementation of the model is based
on a [project on the economics of pandemic
preparedness](https://github.com/robj411/p2_drivers).

## References

Haw, David J., Giovanni Forchini, Patrick Doohan, Paula Christen, Matteo
Pianella, Robert Johnson, Sumali Bajaj, et al. 2022. “Optimizing Social
and Economic Activity While Containing SARS-CoV-2 Transmission Using
DAEDALUS.” *Nature Computational Science* 2 (4): 223–33.
<https://doi.org/10.1038/s43588-022-00233-0>.

# Package index

## Run multiple scenarios

Run multiple Daedalus scenarios.

- [`run_scenarios()`](https://jameel-institute.github.io/daedalus.compare/reference/run_scenarios.md)
  : Run multiple DAEDALUS scenarios

## Output helper functions

Get output for comparison from multiple scenarios’ output.

- [`get_cost_data()`](https://jameel-institute.github.io/daedalus.compare/reference/get_cost_data.md)
  : Get cost data from DAEDALUS scenarios
- [`get_costs_list()`](https://jameel-institute.github.io/daedalus.compare/reference/get_costs_list.md)
  : Get pandemic costs from a list of model outputs
- [`get_econ_cost_data()`](https://jameel-institute.github.io/daedalus.compare/reference/get_econ_cost_data.md)
  : Get economic cost details from an output data.table
- [`get_econ_costs_list()`](https://jameel-institute.github.io/daedalus.compare/reference/get_econ_costs_list.md)
  : Get details of economic costs from model outputs
- [`get_epicurve_data()`](https://jameel-institute.github.io/daedalus.compare/reference/get_epicurve_data.md)
  : Get epidemiological curves from model data
- [`get_epidata_list()`](https://jameel-institute.github.io/daedalus.compare/reference/get_epidata_list.md)
  : Get incidence data from a list of model outputs
- [`get_summary_data()`](https://jameel-institute.github.io/daedalus.compare/reference/get_summary_data.md)
  : Get summary data from DAEDALUS scenarios
- [`get_summary_list()`](https://jameel-institute.github.io/daedalus.compare/reference/get_summary_list.md)
  : Get epidemic summary data from a list of model outputs

## Miscellaneous helper functions

Other helpful functions.

- [`ci()`](https://jameel-institute.github.io/daedalus.compare/reference/ci.md)
  : Calculate an interval

- [`make_infection_samples()`](https://jameel-institute.github.io/daedalus.compare/reference/make_infection_samples.md)
  :

  Generate multiple `<daedalus_infection>`s from parameter distributions

## Package constants

Commonly used constant values

- [`NAMES_VECTOR_INF_PARAMS`](https://jameel-institute.github.io/daedalus.compare/reference/NAMES_VECTOR_INF_PARAMS.md)
  : Names of age-varying infection parameters

# Articles

### All vignettes

- [Comparing outcomes across vaccination
  strategies](https://jameel-institute.github.io/daedalus.compare/articles/comparing_vaccination.md):
- [Getting started with multi-scenario modelling and outcome
  comparisons](https://jameel-institute.github.io/daedalus.compare/articles/daedalus-compare.md):
